"""PAIOS Domain Layer (Milestone 1).

Pure domain model: entities, value objects, enums, state machines,
immutable transition history, domain invariants, and domain services.

Python standard library only. No frameworks, no persistence, no I/O.
The domain model drives the codebase; the codebase must not drive the
domain model (DOMAIN_MODEL.md - Architectural Status).
"""
